## Wave 1
- [x] T1: Export `DalosSecretReveal`, fix the lucide vitest harness, and wire a reveal button per seed row — done when:
  1. `packages/codex-ouronet/src/ui/index.ts` adds `export { DalosSecretReveal } from "./internal/DalosSecretReveal.js";` and `export type { DalosSecretRevealProps } from "./internal/DalosSecretReveal.js";`.
  2. `packages/codex-arweave/vitest.config.ts` gets the same 3-part lucide-react fix already in `packages/codex-ouronet/vitest.config.ts` (read that file's exact `optimizeDeps.exclude`, the `lucide-react` → `resolve(__dirname, "../../node_modules/lucide-react/dist/esm/lucide-react.js")` alias, and `server.deps.inline` addition — copy the same pattern, adjusted for this file's existing `server.deps.inline` array which already lists other packages, so ADD to it rather than replacing it).
  3. In `ArweaveSeedsArea.tsx`'s `SeedRow`, a new button (`data-testid="arweave-seed-reveal-${seed.id}"`, label "View Seed" or similar, small glyph button matching the row's existing button style) sits alongside the existing delete button in the row header. Clicking it opens a local modal overlay (a simple fixed-position centered overlay, mirroring `ArweavePanel.tsx`'s `LEAVE_DIALOG_BACKDROP_STYLE` pattern — no new shared modal component needed) containing `DalosSecretReveal` imported from `@ancientpantheon/codex-ouronet/ui`, with `plaintext={seed.bits}`, `originMode="bitString"`, `originCurve="dalos"`, and a close control (`data-testid="arweave-seed-reveal-close"`).
  4. When `seed.bits === ""`, the modal shows an inline notice (`data-testid="arweave-seed-reveal-locked"`) instead of mounting `DalosSecretReveal` — e.g. "This seed's material could not be decrypted (codex may be locked)." — never renders `DalosSecretReveal` with empty plaintext.
  5. New tests in `packages/codex-arweave/tests/e5-seeds-area.test.tsx`: reveal button present on both prime and non-prime rows; clicking it renders `DalosSecretReveal`'s content (assert on a known tab testid/label it renders, e.g. its BitString tab); the empty-bits case shows the locked notice and never mounts the reveal component; close control dismisses the modal.
  6. `npx vitest run --root packages/codex-arweave` (whole package) shows no new failures beyond the six documented pre-existing `node:sqlite` load failures. `npx vitest run --root packages/codex-ouronet` shows no new failures. `npx tsc --noEmit -p packages/codex-arweave/tsconfig.json` and `npx tsc --noEmit -p packages/codex-ouronet/tsconfig.json` are both clean.
  - files: `packages/codex-ouronet/src/ui/index.ts`, `packages/codex-arweave/vitest.config.ts`, `packages/codex-arweave/src/panel/ArweaveSeedsArea.tsx`, `packages/codex-arweave/tests/e5-seeds-area.test.tsx`

- [x] T2: Prove (and fix if broken) that Quick Define works end-to-end through the REAL playground wiring, not just package-level fakes — done when:
  1. A new test in `apps/codex-playground/tests/e5-foreign-chains-mock.test.tsx`, modeled on the existing `"enables Generate on a freshly defined seed, with no 'not wired' note"` test (same file) and the existing `PG-02`/persist-path tests that drive real crypto through a fake `Worker`: mounts the real wired `ForeignChainsTab` (via the file's existing `renderWiredForeignChainsTab` helper or equivalent), with a codex snapshot carrying at least one activated dalos-curve Ouronet account flagged as prime/CodexPrime, navigates to the Arweave Seeds category with ZERO existing Arweave keys, clicks `arweave-seed-quick-define`, and asserts: (a) the Prime Arweave Seed row becomes defined (`data-defined="true"`) without opening the custom form; (b) exactly one key at index 0 is generated and lands in the real `foreignKeys` store slice (check via the store or via the Accounts view rendering one row); (c) no error is shown.
  2. If this test reveals a REAL gap in the live wiring (as opposed to just needing the test itself to be written), fix it — but do NOT touch `packages/codex-arweave/src/panel/ArweaveSeedsArea.tsx` to do so (that file is owned by T1 in this same wave); if the bug turns out to require changing that file, STOP and report the exact gap instead of editing it.
  3. `npx vitest run --root apps/codex-playground` shows no new failures.
  - files: `apps/codex-playground/tests/e5-foreign-chains-mock.test.tsx`, `apps/codex-playground/src/ForeignChainsWiring.tsx` (only if a real gap is found there)

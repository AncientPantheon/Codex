## Wave 1
- [x] T1: Lift `rebuildFullKey` out of `DalosSecretReveal.tsx` into a reusable exported helper so the Arweave seed picker can turn a stored Ouronet account into its 1600-bit bitstring. Move the existing module-local `rebuildFullKey(plaintext, originMode, originCurve): FullKey | null` verbatim (it already switches over all five origin modes: `seedWords`, `bitmap`, `bitString`, `integerBase10`, `integerBase49`) into a new `rebuildFullKey.ts`, export it from `codex-identity/index.ts`, and have `DalosSecretReveal.tsx` import it instead of declaring it. Behaviour must not change. Also export a `bitStringOf(account, plaintext): string | null` helper that returns `rebuildFullKey(...).privateKey.bitString`, resolving the curve as `account.originCurve ?? (address starts with "Ѻ." or "Σ." ? "dalos" : "apollo")`. — done when: `npx vitest run --root packages/codex-ouronet tests/codex-identity-rebuild-full-key.test.ts` passes with cases asserting (a) a `seedWords`-origin rebuild returns a `FullKey` whose `privateKey.bitString.length === 1600` for curve `dalos`, (b) an `integerBase10`-origin rebuild returns a 1600-bit string, (c) an unparseable plaintext returns `null` rather than throwing, (d) `bitStringOf` infers `dalos` from an `Ѻ.`-prefixed address when `originCurve` is absent; AND `npx vitest run --root packages/codex-ouronet` shows no regression (baseline 762 passed).
  - files: `packages/codex-ouronet/src/codex-identity/rebuildFullKey.ts` (new), `packages/codex-ouronet/src/codex-identity/index.ts`, `packages/codex-ouronet/src/ui/internal/DalosSecretReveal.tsx`, `packages/codex-ouronet/tests/codex-identity-rebuild-full-key.test.ts` (new)

- [x] T2: Give `ForeignKeyEntry` the seed provenance the Accounts grouping needs, without breaking existing exports. Add three OPTIONAL fields to the type in `foreignKeys.ts`: `seedId?: string` (the Arweave seed the key was derived from), `index?: number` (its position in THAT seed's own index space), and `address?: string` (plaintext Arweave address, so a list can render without decrypting). Keep them optional so entries written before this change stay valid. Bump `ForeignKeysBlock.schemaVersion` by 1 and make the structural guard accept BOTH shapes: an entry with none of the new fields is valid (legacy), and an entry with `index` present must have `index` be a non-negative integer and `seedId` a non-empty string. — done when: `npx vitest run --root packages/codex-core` passes with new cases in `tests/codec-foreign-keys-seed-provenance.test.ts` asserting (a) a legacy entry `{id,chainId,encryptedKeyfile}` still passes the guard and survives a serialize→deserialize round-trip unchanged, (b) an entry carrying `seedId`/`index`/`address` survives the same round-trip with all three preserved, (c) an entry with `index: -1` or `index: 1.5` is rejected by the guard, (d) an entry with `index` but no `seedId` is rejected; AND the existing `tests/codec-1-3-matrix.test.ts` still passes.
  - files: `packages/codex-core/src/codex/foreignKeys.ts`, `packages/codex-core/src/codex/codec.ts`, `packages/codex-core/tests/codec-foreign-keys-seed-provenance.test.ts` (new)

- [x] T3: Create the seed resolver that turns each of the three UI sources into ONE validated 1600-bit bitstring — the single input to the deterministic RSA search. Export `resolveSeedBitString(input): { bits: string } | { error: string }` where `input` is a discriminated union: `{ kind: "words"; words: string[] }` (Options 1a/1b and Option 3 — run gen1's `validateSeedWords` first, then `seedWordsToBitString(words)`), or `{ kind: "bitstring"; bits: string }` (Option 2 — an Ouronet account's already-1600 bitstring). Import `seedWordsToBitString` and `validateSeedWords` from `@ouronet/dalos-crypto/gen1`. Enforce EXACTLY 1600 bits on the way out and REFUSE anything else — never pad, never truncate. A `validateSeedWords` rejection must be surfaced as a human-readable `error` naming the offending word, not thrown. — done when: `npx vitest run --root packages/codex-arweave tests/e5-seed-resolve.test.ts` passes with cases asserting (a) `{kind:"words", words:["hello","world"]}` returns exactly 1600 bits of `/^[01]+$/`, (b) a real 12-word BIP39 phrase (`"abandon ability able about above absent absorb abstract absurd abuse access accident"`) returns 1600 bits — the Option 3 path, (c) a word containing a character outside the DALOS 256-glyph set returns an `error` mentioning that word and does NOT throw, (d) 257 words returns an `error`, (e) `{kind:"bitstring"}` with a 1024-bit string returns an `error` (Apollo is deferred) rather than being padded, (f) `{kind:"bitstring"}` with exactly 1600 valid bits passes through unchanged.
  - files: `packages/codex-arweave/src/seeds/resolveSeedBitString.ts` (new), `packages/codex-arweave/src/seeds/index.ts` (new), `packages/codex-arweave/tests/e5-seed-resolve.test.ts` (new), `packages/codex-arweave/package.json` (orchestrator repair: the package declared NO `@ouronet/dalos-crypto`, so it silently resolved the root-hoisted 4.0.4 whose `/gen1` predates `validateSeedWords`; added peer `>=4.4.0` + dev `^4.4.0`)

- [x] T4: Create the pure range planner implementing "skip already-populated positions" per `docs/HANDOFF-rsa4096-skip-populated-indices.md`. Export `planIndexRanges({ selection, existing, cap }): { ranges: Array<{start:number;end:number}>; requested: number[]; skipped: number[] } | { error: string }`. `selection` is a discriminated union matching the four UI modes: `{kind:"default"}` → `[0]`; `{kind:"single"; position:number}` → `[0, position]`; `{kind:"upTo"; n:number}` → `[0..n]`; `{kind:"ranges"; ranges:Array<{start:number;end:number}>}` → the union of those ranges plus 0. Expand the selection to a deduplicated ascending index list (`requested`), partition it against `existing: number[]` into the ones to generate and `skipped`, and emit `ranges` as singleton `{start:i,end:i}` entries for the ones to generate. Return an `error` when the count of indices to generate exceeds `cap`, when any index is negative or non-integer, or when a range has `end < start`. Document in a comment that index 0 is force-generated by the library regardless of these ranges, so this planner is an optimisation and NOT the correctness guard. Do NOT touch `packages/codex-arweave/src/seeds/index.ts` — T3 owns that barrel in the same wave; consumers import `planIndexRanges.ts` directly. — done when: `npx vitest run --root packages/codex-arweave tests/e5-plan-ranges.test.ts` passes with cases asserting (a) `{kind:"default"}` with `existing:[]` yields `ranges:[{start:0,end:0}]`, (b) `{kind:"upTo", n:5}` with `existing:[2,4]` yields ranges for exactly `[0,1,3,5]` and `skipped:[2,4]`, (c) index 0 already in `existing` is reported in `skipped` and omitted from `ranges`, (d) overlapping input ranges are deduplicated so no index appears twice, (e) a selection expanding to `cap+1` indices returns an `error`, (f) `{kind:"ranges"}` with `end < start` returns an `error`.
  - files: `packages/codex-arweave/src/seeds/planIndexRanges.ts` (new), `packages/codex-arweave/tests/e5-plan-ranges.test.ts` (new)

## Wave 2 (depends on Wave 1)
- [x] T5: Add the SEEDED batch path to the off-main-thread keygen. Today `packages/codex-arweave/src/keygen/worker.ts` lazily imports arweave-core's RANDOM `generateKey()`; add a second message kind that instead drives `generateFromBitStringAtRangesAsync` from `@ouronet/dalos-crypto/rsa4096` over a supplied 1600-bit bitstring plus the ranges produced by T4's `planIndexRanges`. Extend `KeygenWorkerMsg` with `{kind:"key"; index:number; jwk:ArweaveJwk; address:string}` emitted per generated key, and a `{kind:"cancelled"}` terminal message; keep the existing `progress`/`done`/`error` kinds working so the current random path is unaffected. Export `runSeededBatch({ bits, ranges, workerFactory, onKey, onProgress, signal })` from `KeygenRunner.ts`: it must invoke `onKey` for EVERY key as it arrives (never buffering until the end, so a cancel or closed tab keeps completed work), and stop generating when `signal` is aborted. Per the handoff, index 0 is force-generated by the library on every call regardless of `ranges` — so `runSeededBatch` must pass every result to `onKey` and let the CALLER decide whether to store it; do not filter index 0 here. — done when: `npx vitest run --root packages/codex-arweave tests/e5-seeded-batch.test.ts` passes against a FAKE worker (no real RSA, no real Worker — jsdom has none) with cases asserting (a) each `key` message is delivered to `onKey` as it arrives, in order, (b) aborting the `signal` mid-stream stops delivery and the keys already delivered are NOT rolled back, (c) a `{kind:"error"}` message rejects the returned promise with that message, (d) the pre-existing random `runKeygen` path still resolves a jwk from a `done` message; AND `npx vitest run --root packages/codex-arweave` shows no regression beyond the 6 known `node:sqlite` load failures.
  - files: `packages/codex-arweave/src/keygen/worker.ts`, `packages/codex-arweave/src/keygen/KeygenRunner.ts`, `packages/codex-arweave/tests/e5-seeded-batch.test.ts` (new)

- [x] T6: Build the Arweave Accounts list as a presentational component, so generated keys are visible grouped by the seed that produced them. Export `ArweaveAccountsArea({ entries, seeds })` where `entries: ForeignKeyEntry[]` (carrying T2's optional `seedId`/`index`/`address`) and `seeds: Array<{ id: string; label: string }>`. Render one collapsible group per seed containing that seed's entries sorted ascending by `index`, each row showing `#<index>` and the `address` (never decrypting anything). Entries whose `seedId` matches no known seed, and legacy entries with no `seedId`, both go into a final "Unassigned" group rather than being dropped. Render an explicit empty state when `entries` is empty. Use INLINE SVG for any icon — `lucide-react` must NOT be imported in codex-arweave (it resolves the repo-root React 18 and throws "A React Element from an older version of React"). — done when: `npx vitest run --root packages/codex-arweave tests/e5-accounts-area.test.tsx` passes with cases asserting (a) two entries under one seed render in ascending `index` order regardless of array order, (b) an entry with an unknown `seedId` and a legacy entry with no `seedId` both appear under "Unassigned", (c) each row shows its index and address text, (d) an empty `entries` array renders a visible empty state and does not throw, (e) the rendered DOM contains no `encryptedKeyfile` value.
  - files: `packages/codex-arweave/src/panel/ArweaveAccountsArea.tsx` (new), `packages/codex-arweave/tests/e5-accounts-area.test.tsx` (new)

## Wave 3 (depends on Wave 2)
- [x] T7: Build the Seeds category surface and mount both wired categories in the panel. Create `ArweaveSeedsArea` rendering: a Prime Arweave Seed row ALWAYS first (shown as undefined with a `Define Prime Arweave Seed` action when no prime seed exists, even in an empty Codex), then any further seeds, each expandable to show its generated addresses or an "unused" state with a generate action. The define-seed flow offers the three sources from design.md — (1) typed words with a Free Seed Input default and a BIP-dictionary-gated variant, (2) an activated `dalos`-curve Ouronet account (default CodexPrime, using T1's `bitStringOf`), (3) an existing Chainweb seed (default the Prime Codex Seed) — and each of options 2 and 3 is INDEPENDENTLY disabled when that source has no entries, since either can exist without the other. All three route through T3's `resolveSeedBitString`, surfacing its `error` text inline. The generate action offers the four position modes, feeds T4's `planIndexRanges` with a cap of **100 while the Codex holds zero Arweave keys and 1000 once at least one exists** (Codex-wide, any seed), then runs T5's `runSeededBatch`, persisting each key via `onKey` as it arrives with a never-clobber guard (`if (existing.has(index)) return;` — applied to EVERY index, because the library force-generates index 0 regardless of ranges), showing live progress and a cancel control. Finally, replace the `seeds` and `accounts` placeholders in `ArweavePanel.tsx` with `ArweaveSeedsArea` and T6's `ArweaveAccountsArea`; the other three categories keep their existing empty placeholders. Use INLINE SVG only — never import `lucide-react` in codex-arweave. — done when: `npx vitest run --root packages/codex-arweave tests/e5-seeds-area.test.tsx` passes with cases asserting (a) with zero seeds, a Prime Arweave Seed row renders first with a `Define Prime Arweave Seed` control, (b) option 2 is disabled when no activated dalos-curve account exists AND option 3 is independently disabled when no Chainweb seed exists, (c) a word outside the DALOS charset shows the resolver's error inline and no seed is stored, (d) with zero Arweave keys a request for 101 positions is refused and the surfaced limit reads 100, (e) with one existing Arweave key a request for 1000 is accepted and 1001 is refused, (f) a generated key already present at that index is NOT overwritten — including index 0 — while new indices are stored, (g) cancelling mid-run keeps the keys already delivered; AND `npx vitest run --root packages/codex-arweave` regresses nothing beyond the 6 known `node:sqlite` load failures, AND the root `npm run build` exits 0.
  - files: `packages/codex-arweave/src/panel/ArweaveSeedsArea.tsx` (new), `packages/codex-arweave/src/panel/ArweavePanel.tsx`, `packages/codex-arweave/tests/e5-seeds-area.test.tsx` (new)

## Wave 4 (app wiring + gating — depends on Wave 3)
- [x] T8: Fill the Arweave panel's injected seams with REAL codex data so the Seeds and Accounts categories operate on the actual Codex instead of fixtures, and give the generate flow a real off-main-thread worker. `ArweaveSeedsArea` already declares every prop needed — this task supplies them, it does not change the component. Supply from the codex store via the existing hooks: activated Ouronet accounts filtered to `curveOf(account) === "dalos"` (`useOuroAccounts` + `curveOf` from `@ancientpantheon/codex-ouronet/codex-identity`) for Option 2; StoaChain seeds (`useStoaChainSeeds`) for Option 3; and the real `foreignKeys` slice for Accounts. Wire a seeded `workerFactory` the same way `realArweaveAdapter.ts`'s existing `createKeygenWorker` does — `new Worker(new URL(...), { type: "module" })` resolved at APP level, never hardcoded inside the package (`worker.ts` vs `worker.js` resolve differently in src vs dist). ALSO delete the hardcoded demo entry at `apps/codex-playground/src/mockArweaveAdapter.ts:70` (`id: "mock-arweave-key-1"`, label `"Mock Arweave key"`): it currently renders in Accounts under "Unassigned" and makes an empty Codex look populated. — done when: `npx vitest run --root apps/codex-playground` passes (baseline 76) with new cases asserting (a) with no Arweave keys in the store, Accounts renders the empty state and NO "Mock Arweave key" text appears anywhere, (b) Option 2 lists a dalos-curve Ouronet account from the store and does NOT list an apollo-curve one, (c) Option 3 lists a StoaChain seed from the store; AND `npm run dev` for the playground serves on a free port with no transform error.
  - files: `apps/codex-playground/src/ForeignChainsWiring.tsx`, `apps/codex-playground/src/realArweaveAdapter.ts`, `apps/codex-playground/src/mockArweaveAdapter.ts`, `apps/codex-playground/tests/e5-foreign-chains-mock.test.tsx`

- [x] T9: Make Restricted Seed Input actually restricted. Today it accepts arbitrary words, which makes it indistinguishable from Free Seed Input. Gate every typed word against the BIP English wordlist from `@scure/bip39/wordlists/english` (already present in the tree via `@stoachain/stoa-core`, but codex-arweave declares NO dependency on it — ADD `@scure/bip39` to `packages/codex-arweave/package.json` peer + dev at the version stoa-core pins, `1.2.1`, or the import silently resolves the root hoist, which is the exact defect that broke T3). Typing MUST stay allowed (it is one of the two required variants); rejection is per-word and must name the offending word. Keep the generate-a-random-phrase variant working. Free Seed Input (1a) is NOT affected — it stays DALOS-charset-only. — done when: `npx vitest run --root packages/codex-arweave tests/e5-seeds-area.test.tsx` passes with new cases asserting (a) a phrase of valid BIP words is accepted by the Restricted variant, (b) a phrase containing `zzzznotaword` is rejected with an inline error naming that word and no seed is stored, (c) the same non-dictionary phrase IS accepted by Free Seed Input (proving the two variants differ), (d) the generate variant produces a phrase that its own validator accepts; AND `npx vitest run --root packages/codex-arweave` regresses nothing beyond the 6 known `node:sqlite` loads.
  - files: `packages/codex-arweave/src/panel/ArweaveSeedsArea.tsx`, `packages/codex-arweave/package.json`, `packages/codex-arweave/tests/e5-seeds-area.test.tsx`

## Wave 5 (depends on Wave 4)
- [x] T10: Let a seed be deleted, cascading to every RSA key derived from it. Add a delete control per seed row in `ArweaveSeedsArea`; on confirm it must remove the seed AND every `ForeignKeyEntry` whose `seedId` matches, via an injected callback (the component stays presentational — no direct store access). No orphaned key may survive in Accounts. The Prime Arweave Seed is deletable FOR NOW: render the same delete control on it, but the define-Prime flow must show a warning at definition time stating the Prime seed is permanent in the shipped build — the copy ships now, the block does not. Deleting a seed mid-generation must also abort any run in flight against it. — done when: `npx vitest run --root packages/codex-arweave tests/e5-seeds-area.test.tsx` passes with new cases asserting (a) deleting a seed invokes the callback with that seed id AND the ids of exactly its own keys, leaving another seed's keys untouched, (b) the Prime seed row exposes a delete control (dev behaviour), (c) the define-Prime flow renders permanence warning copy before confirmation, (d) deleting a seed with a run in flight aborts that run; AND `npx vitest run --root packages/codex-arweave` regresses nothing beyond the 6 known `node:sqlite` loads.
  - files: `packages/codex-arweave/src/panel/ArweaveSeedsArea.tsx`, `packages/codex-arweave/tests/e5-seeds-area.test.tsx`

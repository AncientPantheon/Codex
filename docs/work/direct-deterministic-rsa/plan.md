## Wave 1
- [x] T1: Wire the Direct Deterministic RSA Generation mode (width toggle, BitString, Base-10/49 with auto-width-correction, Bitmap via `BitmapKeyInput`, `confirmDefine` branch, `bitLength` on `ArweaveSeedRecord`) — done when: every acceptance criterion in `docs/work/direct-deterministic-rsa/design.md` is met and covered by a passing test; `npx vitest run --root packages/codex-arweave` shows no new failures beyond the six documented pre-existing `node:sqlite` load failures; `npx tsc --noEmit -p packages/codex-arweave/tsconfig.json` is clean.
  - files: `packages/codex-arweave/src/panel/ArweaveSeedsArea.tsx`, `packages/codex-arweave/tests/e5-seeds-area.test.tsx`, `packages/codex-arweave/package.json` (only if a new dependency subpath needs declaring — check first)

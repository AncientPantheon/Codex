## Wave 1
- [x] T1: Relayout `ForeignChainsTab` from a horizontal strip to a vertical left rail with a count-gated search field. Keep the injected contract (`foreignChains`, `foreignChainPanels`, `ctx`) and the id-blind behaviour exactly as-is — no chain-specific branch. Layout becomes a two-column flex: rail left (fixed ~190px, `overflow-y:auto`, `role="tablist"` with vertical orientation), active panel right. Render a search `<input>` ABOVE the rail only when `foreignChains.length > 6`; typing filters the rail case-insensitively by id; a filter matching nothing shows "No matching chains" in the rail while the active panel stays mounted. — done when: `npx vitest run --root packages/codex-ui tests/foreign-chains-tab.test.tsx` passes with new cases asserting (a) no `<input>` exists for a 2-chain list, (b) an `<input>` exists for a 7-chain list, (c) typing in it narrows the rendered rail buttons, (d) the existing empty-list "No foreign chains." and missing-panel fallback assertions still pass unchanged.
  - files: `packages/codex-ui/src/ui/foreign-chains/ForeignChainsTab.tsx`, `packages/codex-ui/tests/foreign-chains-tab.test.tsx`

- [x] T2: Create `ChainwebPanel` — the Chainweb chain panel that re-parents the three EXISTING Chainweb tabs unchanged under three categories. It renders its own horizontal category strip with buttons labelled `Seeds`, `Pure Keys`, `Accounts` (ids `seeds`, `pure-keys`, `accounts`) and mounts `<SeedWordsTab />`, `<PureKeypairsTab />`, `<StoaAccountsTab />` respectively, each with NO prop changes and NO copied logic. Default category is `accounts`. It accepts the `PanelProps` shape (`{ chainId, ctx }`) that `ForeignChainsTab` passes, and ignores both. Export it from `packages/codex-ouronet/src/ui/index.ts`. — done when: `npx vitest run --root packages/codex-ouronet tests/ui-chainweb-panel.test.tsx` passes with cases asserting three category buttons render with those exact labels, `accounts` is selected on first render, and clicking `Seeds` then `Pure Keys` mounts the seed-words and pure-keypairs surfaces respectively.
  - files: `packages/codex-ouronet/src/ui/chains/ChainwebPanel.tsx` (new), `packages/codex-ouronet/src/ui/index.ts`, `packages/codex-ouronet/tests/ui-chainweb-panel.test.tsx` (new)

- [x] T3: Restructure `ArweavePanel`'s subtabs from `keyring/balance/send/upload/library` to the five categories `seeds`, `pure-keys`, `accounts`, `upload`, `library` with labels `Seeds`, `Pure Keys`, `Accounts`, `Upload`, `Library`. Mapping: `pure-keys` renders the existing `<KeyringArea>` with its current props; `accounts` renders `<BalanceArea>` above `<SendArea>` with their current props; `upload` renders the existing `<UploadArea>`; `library` renders the existing `<LibraryArea>`. `seeds` renders a placeholder `<div>` whose text contains `not yet wired` — it must never render blank and never throw. Default category is `accounts`. `data-testid` becomes `arweave-subtab-<categoryId>`. — done when: `npx vitest run --root packages/codex-arweave` passes, including a new `tests/e4-panel-categories.test.tsx` asserting all five buttons render with those labels, that selecting `seeds` shows text matching `/not yet wired/i`, and that selecting `accounts` renders both the balance and send surfaces; and the existing `e4-panel-keyring/-balance/-send/-upload/-library` tests plus `e4-integration-smoke.test.tsx` all pass (update the smoke test's subtab ids to the new vocabulary).
  - files: `packages/codex-arweave/src/panel/ArweavePanel.tsx`, `packages/codex-arweave/tests/e4-panel-categories.test.tsx` (new), `packages/codex-arweave/tests/e4-integration-smoke.test.tsx`

- [x] T4: Add Arweave as a fourth Address Book sub-tab. Widen the entry union at `packages/codex-ouronet/src/types/entities.ts` line ~214 from `"ouronet" | "stoa" | "stoic-tag"` to additionally include `"arweave"`. In `AddressBookTab.tsx` add an `arweave` entry to `TAB_CONFIG` (label `Arweave`, accent `#f0a500`, addressLabel `Address`, placeholder `43-character Arweave address…`, hint `A 43-character base64url Arweave address`, emptyTitle `No Arweave Addresses`, emptyAction `Arweave Address`, `validate: (v) => /^[A-Za-z0-9_-]{43}$/.test(v.trim())`) and append `"arweave"` to `TAB_ORDER`. Arweave entries render through the existing non-Ouronet path (plain `MiddleEllipsis`), so no new address component. — done when: `npx vitest run --root packages/codex-ouronet tests/ui-address-book-tab.test.tsx` passes with new cases asserting a fourth sub-tab button labelled `Arweave` exists, that saving `aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa` (43 chars) adds an entry retrievable via `getByTitle`, and that saving `not-an-address` does not add an entry; and the 6 existing tests in that file still pass.
  - files: `packages/codex-ouronet/src/types/entities.ts`, `packages/codex-ouronet/src/ui/tabs/AddressBookTab.tsx`, `packages/codex-ouronet/tests/ui-address-book-tab.test.tsx`

## Wave 2 (depends on Wave 1)
- [x] T5: Collapse `CodexTabs`' five top-level tabs into three Class tabs. `TAB_ITEMS` becomes exactly: `ouronet-accounts` (label `Ouronet Accounts`, unchanged content), `blockchain-accounts` (label `Blockchain Accounts`, icon `Boxes`, accent `#f0a500`), and `address-book` (label `Address Book`, unchanged content). The `seed-words`, `pure-keypairs`, and `stoa-accounts` items are REMOVED from the top level — their components are no longer imported here (they are reached via `ChainwebPanel` from T2). `blockchain-accounts` renders `<ForeignChainsTab foreignChains={...} foreignChainPanels={...} />` fed from two NEW optional `CodexTabsProps` fields, `foreignChains?: string[]` (default `[]`) and `foreignChainPanels?: ForeignChainPanels` (default `{}`), so `codex-ouronet` gains NO dependency on `codex-arweave`. With the defaults, Class 2 renders `ForeignChainsTab`'s existing "No foreign chains." empty state rather than crashing. `defaultTab` default stays `"ouronet-accounts"`. — done when: `npx vitest run --root packages/codex-ouronet tests/ui-codex-tabs.test.tsx` passes with cases asserting exactly three top-level tab buttons render with those three labels, that no button labelled `Seed Words`, `Pure Key Pairs`, or `Stoa Accounts` exists at the top level, and that rendering `<CodexTabs />` with no foreign-chain props and selecting `Blockchain Accounts` shows `No foreign chains.` without throwing.
  - files: `packages/codex-ouronet/src/ui/CodexTabs.tsx`, `packages/codex-ouronet/tests/ui-codex-tabs.test.tsx`

## Wave 3 (depends on Wave 2)
- [x] T6: Wire the two chain panels together in the `codex` aggregate, which is the only package that depends on all four members. In `packages/codex/src/ui/index.ts` export a `CodexTabsWired` component that renders `CodexTabs` with `foreignChains={["chainweb", "arweave"]}` and `foreignChainPanels={{ chainweb: ChainwebPanel, arweave: ArweavePanel }}` (importing `ArweavePanel` from `@ancientpantheon/codex-arweave/panel`), forwarding `className` and `defaultTab`. Also call `registerArweaveAddressValidator()` once at module scope, guarded by a `getRegisteredChains().includes(ARWEAVE_CHAIN_ID)` check so repeated module evaluation does not re-register. Keep the existing `CodexTabs` export as-is for consumers that wire their own chains. — done when: `npm run build` from the repo root exits 0 (the codex-ouronet/codex-ui circular pair must be built via the root script, never standalone), and `node -e "import('@ancientpantheon/codex/ui').then(m => { if (!m.CodexTabsWired) process.exit(1) })"` run from `packages/codex` exits 0.
  - files: `packages/codex/src/ui/CodexTabsWired.tsx` (new), `packages/codex/src/ui/index.ts`

- [x] T7: Give the local playground a REAL off-main-thread keygen so the Arweave generate button is testable. In `apps/codex-playground/src/realArweaveAdapter.ts` replace the main-thread throwing no-op `KeygenRunner` with `createWorkerKeygenRunner({ workerFactory: () => new Worker(new URL("@ancientpantheon/codex-arweave/keygen/worker", import.meta.url), { type: "module" }) })`, resolving the worker entry to the built `codex-arweave` keygen worker (add a Vite alias in `apps/codex-playground/vite.config.ts` if the bare specifier does not resolve). Update the file's S-3 "keygen unsupported" comment to state that worker keygen is now wired for local dev. Mount Class 2 in the playground by passing `foreignChains`/`foreignChainPanels` through `ForeignChainsWiring.tsx`. ALSO replace the hardcoded `addressBook: []` in BOTH `mockArweaveAdapter.ts` and `realArweaveAdapter.ts` with the codex address-book entries mapped to `{ id, name, address, chainId }`, setting `chainId` to `ARWEAVE_CHAIN_ID` for `type === "arweave"` entries — `SendArea` already filters the seam by `chainId === ARWEAVE_CHAIN_ID`, so an empty seam is why saved Arweave addresses would never appear in the recipient picker. ALSO update `apps/codex-playground/tests/e5-foreign-chains-mock.test.tsx`, which still clicks the OLD subtab testids (`arweave-subtab-keyring`, `-balance`, `-send`) at lines ~78/90/103/112/121/138: rename `keyring`→`pure-keys` and `balance`/`send`→`accounts`, and account for the default category having moved from `keyring` to `accounts` (a test that relied on Keyring being selected on first render must now click `arweave-subtab-pure-keys` first). — done when: `npx vitest run --root apps/codex-playground` passes, `npm run dev` for the playground serves on :3007 without a build error, and clicking Create-new-key under Arweave → Pure Keys in REAL mode produces a new key whose address is 43 characters and matches `/^[A-Za-z0-9_-]{43}$/`, with the tab strip remaining responsive (no frozen main thread) during generation; and an Arweave address saved in the Address Book appears in the Send recipient picker.
  - files: `apps/codex-playground/src/realArweaveAdapter.ts`, `apps/codex-playground/src/mockArweaveAdapter.ts`, `apps/codex-playground/src/ForeignChainsWiring.tsx`, `apps/codex-playground/vite.config.ts`, `apps/codex-playground/tests/e5-foreign-chains-mock.test.tsx`

## Wave 4 (repair — plan drift found during Wave 3)
- [x] T8: Restore a working verification bar for the playground suite, and migrate the three test files that still assert the pre-T5 five-tab top level. THREE separate causes, all confirmed: (1) `apps/codex-playground/vitest.config.ts` cannot LOAD 5 of 9 test files — `Cannot bundle built-in module "node:sqlite" imported from packages/codex-arweave/src/library/sqliteStore.ts`; reproduced at HEAD via `git archive`, so it predates this topic. Add a `node:sqlite` stub alias (or equivalent `server.deps`/`noExternal` exclusion) in `vitest.config.ts` ONLY — do not touch `sqliteStore.ts` or codex-arweave. (2) `__CODEX_VERSION__` is defined only in `vite.config.ts`, which vitest ignores when `vitest.config.ts` exists, so every App-rendering test throws `ReferenceError: __CODEX_VERSION__ is not defined`; add the same `define` to `vitest.config.ts`. (3) T5 removed the top-level `Seed Words` / `Pure Key Pairs` / `Stoa Accounts` tabs, so `tests/app-load-modes.test.tsx` (4 failures), `tests/e5-standalone-smoke.test.tsx` (2) and `tests/network-settings-wiring.test.tsx` (2) all fail with ``Unable to find role="tab" and name `/seed words/i` ``. Those three surfaces now live under Blockchain Accounts → Chainweb → Seeds / Pure Keys / Accounts, so each assertion must either target the new three-Class top level or navigate Blockchain Accounts → `chainweb` rail entry → the `chainweb-subtab-seeds` category. Do NOT re-add any removed tab. — done when: `npx vitest run --root apps/codex-playground` loads all 9 files and reports 0 failures.
  - files: `apps/codex-playground/vitest.config.ts`, `apps/codex-playground/tests/app-load-modes.test.tsx`, `apps/codex-playground/tests/e5-standalone-smoke.test.tsx`, `apps/codex-playground/tests/network-settings-wiring.test.tsx`

## Wave 5 (repair — Class 2 not actually mounted in the playground shell)
- [x] T9: Make the playground shell actually render the new Class 2 instead of two disconnected surfaces. TODAY `apps/codex-playground/src/App.tsx:252` renders a BARE `<CodexTabs />` with no `foreignChains`/`foreignChainPanels`, so selecting **Blockchain Accounts** shows `No foreign chains.`, while the working rail lives in a SEPARATE legacy `<section className="cxpg-foreign" aria-label="Foreign chains">` below it (App.tsx ~line 256) that mounts `ForeignChainsWiring` independently. Fix: have `ForeignChainsWiring` render `<CodexTabs foreignChains={...} foreignChainPanels={...} />` INSIDE its `ArweavePanelProvider` (it already owns the registry, the panel map and the address-book mapping), and have `App.tsx` render that single wired surface in place of BOTH the bare `<CodexTabs />` and the duplicate rail section. Keep `<ArweaveModeToggle>` — it is a playground-only dev control — rendered above the tabs so the mock⇄real switch still works. Remove the now-duplicated `<section aria-label="Foreign chains">` wrapper and its `<h2>Foreign chains</h2>` heading, since Class 2 is now the rail's home. Update the two test files that navigate via the removed region: `tests/app-load-modes.test.tsx`'s `openChainwebSeeds` helper currently scopes to `screen.getByRole("region", { name: "Foreign chains" })` and must instead select the `Blockchain Accounts` Class tab first, then the `chainweb` rail entry, then `chainweb-subtab-seeds`; `tests/e5-foreign-chains-mock.test.tsx` mounts `ForeignChainsWiring` directly and must account for it now rendering the full tab shell. — done when: `npx vitest run --root apps/codex-playground` reports 9 files / 0 failures, AND a test asserts that selecting the `Blockchain Accounts` Class tab reveals the `chainweb` and `arweave` rail entries (i.e. the design criterion "Selecting Blockchain Accounts shows a vertical list of blockchains" is satisfied in the real shell, not just in an isolated mount).
  - files: `apps/codex-playground/src/App.tsx`, `apps/codex-playground/src/ForeignChainsWiring.tsx`, `apps/codex-playground/tests/app-load-modes.test.tsx`, `apps/codex-playground/tests/e5-foreign-chains-mock.test.tsx`

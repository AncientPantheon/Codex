## Wave 1
- [x] T1: Gold Prime styling + separator (B), Seed-Based/Direct toggle scaffold (C), and Quick/Custom define split with Quick's one-click orchestration (D), plus exposing live-run status to the parent (needed by T2) — done when:
  1. In `SeedRow` (in `ArweaveSeedsArea.tsx`), the prime row's outer container border becomes `#ceac5f40` (non-prime unchanged `#262626`); the label span color becomes `#ceac5f` when `isPrime`; the toggle's chevron glyph is replaced by a small lock glyph (new local `LockGlyph`, inline SVG, following the file's `svgBase` convention) when `isPrime`, keeping the row's click-to-toggle behavior unchanged; a `🔒 Prime` badge (matching `SeedWordsTab.tsx`'s exact style: `fontSize:10, fontWeight:600, color:"#ceac5f", backgroundColor:"#ceac5f20", borderRadius:9999, padding:"2px 8px"`) renders next to the label when `isPrime`.
  2. A new `PrimeSeparator` component (ported from `SeedWordsTab.tsx` lines 184-192 verbatim: two flex-1 1px `#262626` bars flanking a small uppercase `#555` label) renders in the top-level seed list, immediately after the prime seed's `SeedRow`, labelled `"Other Seeds"`, and ONLY when `seeds.length > 1`.
  3. `DefineSeedForm` gains a segmented toggle in its header area (upper-right), two options: `"Seed-Based Deterministic RSA Generation"` (default, `data-testid="arweave-seed-mode-seed-based"`) and `"Direct Deterministic RSA Generation"` (`data-testid="arweave-seed-mode-direct"`). A new local state `generatorMode: "seed-based" | "direct"` (default `"seed-based"`) controls it. When `"seed-based"`, the form renders EXACTLY as today (the existing `role="radiogroup"` source picker for words/account/chainweb, unchanged). When `"direct"`, the form renders a visible placeholder body (`data-testid="arweave-seed-direct-placeholder"`) listing the four input kinds — `"BitString"`, `"Bitmap"`, `"Base-10 scalar"`, `"Base-49 scalar"` — as labelled, disabled tiles, with a line of copy noting this mode is not wired yet. No crash, no blank body, and Confirm is disabled while `generatorMode === "direct"`.
  4. The top-level empty-state row (where `primeSeed === undefined`) is gated on a NEW condition — `existingKeys.length === 0` (rather than solely `primeSeed === undefined`, since an imported/legacy key with no seed still counts as "an Arweave account exists") — and when that condition holds, renders TWO buttons side by side instead of one: `data-testid="arweave-seed-quick-define"` labelled `"Quick Define"`, and `data-testid="arweave-seed-custom-define"` labelled `"Custom Define Arweave Seed"` (same handler `openDefine` as today's single button, i.e. opens `DefineSeedForm`). When `existingKeys.length > 0` OR `primeSeed !== undefined`, behave exactly as today (no Quick button ever renders again). "Quick Define" is disabled with a `title` tooltip when there is no `dalosAccounts` entry with `isDefault === true`, or `revealAccountSecret`, `workerFactory`, or `persistKey` is undefined.
  5. `startRun` gains an optional second parameter: `startRun(seed: ArweaveSeedRecord, explicitSelection?: IndexSelection)`. Inside, `buildSelection()` is called ONLY when `explicitSelection` is `undefined`; otherwise `explicitSelection` is used directly as the selection fed to `runSeededBatch`'s planning step. Every existing call site (`onRun={() => void startRun(seed)}`) is unaffected (it omits the second argument).
  6. A new callback `handleQuickDefine` (invoked by the Quick Define button): finds `dalosAccounts.find(a => a.isDefault === true)`; if absent, sets `defineError` and returns; else calls `revealAccountSecret(entry.id)`, then `bitStringOf(entry.account, plaintext)`, then `resolveSeedBitString({kind:"bitstring", bits})` — on any failure at any of these steps, sets the SAME inline error state the Custom form uses (`defineError`) and returns, doing nothing else. On success, builds an `ArweaveSeedRecord` exactly as `confirmDefine` does for the prime case (`id: nextSeedId()`, `label: PRIME_SEED_LABEL`, `bits`, `isPrime: true`), adds it to `localSeeds`, calls `onSeedDefined?.(record)`, THEN calls `startRun(record, {kind:"default"})` directly (not via the Custom form's `mode` state) so exactly position 0 is generated. Shows a busy state on the button while in flight (reuse or mirror the existing `defining0` state).
  7. `ArweaveSeedsArea` exposes the run's live status to its parent via a NEW optional prop `onRunActivityChange?: (activity: { seedLabel: string; cancel: () => void } | null) => void`, invoked (via a `useEffect` watching `run`) with a non-null value whenever `run?.status === "running"`, and `null` whenever it is not (including on definition/cleanup). The `cancel` closure calls the SAME logic `cancelRun` already runs. This prop is additive/optional — omitting it changes nothing about existing behavior.
  8. New tests added to `packages/codex-arweave/tests/e5-seeds-area.test.tsx` covering: prime row's gold border/label/lock/badge; the separator's presence with 2+ seeds and absence with exactly 1; the Seed-Based/Direct toggle switching bodies without crashing and Confirm disabled under Direct; the Quick/Custom two-button entry point appearing with zero accounts and disappearing (Quick only) once one exists; Quick Define's one-click define+generate producing exactly one key at index 0 (drive the injected `workerFactory`'s fake worker exactly as the file's other generate-flow tests do); and `onRunActivityChange` firing non-null while a run is in progress and `null` once it settles.
  9. `npx vitest run --root packages/codex-arweave tests/e5-seeds-area.test.tsx` passes in full. `npx vitest run --root packages/codex-arweave` (whole package) shows no new failures beyond the six documented pre-existing `node:sqlite` load failures. `npx tsc --noEmit -p packages/codex-arweave/tsconfig.json` is clean.
  - files: `packages/codex-arweave/src/panel/ArweaveSeedsArea.tsx`, `packages/codex-arweave/tests/e5-seeds-area.test.tsx`

## Wave 2 (depends on Wave 1)
- [x] T2: Navigation-away warning for an in-flight generation (A) — done when:
  1. `ArweavePanel.tsx` reads T1's new `onRunActivityChange` prop by passing a handler into `<ArweaveSeedsArea>` that stores the received `{ seedLabel, cancel } | null` in local state (`const [activeGeneration, setActiveGeneration] = useState<{seedLabel:string; cancel:() => void} | null>(null)`).
  2. Clicking a category tab (`arweave-subtab-*`) OTHER than `seeds` while `activeGeneration !== null` does NOT immediately switch — instead it opens a confirm dialog (`data-testid="arweave-leave-generation-dialog"`) with copy naming the seed (`activeGeneration.seedLabel`), and two actions: `data-testid="arweave-leave-generation-stop"` ("Stop generation now") which calls `activeGeneration.cancel()`, clears `activeGeneration`, THEN switches to the requested category; and `data-testid="arweave-leave-generation-continue"` ("Continue in background") which switches to the requested category WITHOUT calling `cancel()`. Clicking `seeds` itself, or clicking a tab while `activeGeneration === null`, switches immediately as today, no dialog.
  3. A `window.addEventListener("beforeunload", ...)` effect is registered whenever `activeGeneration !== null` (and removed when it becomes `null` or on unmount), calling `event.preventDefault(); event.returnValue = "";` per the standard pattern — this is a native browser prompt, not the custom dialog.
  4. A new test in `packages/codex-arweave/tests/e5-seeds-area.test.tsx` (or a new focused test file if that one is judged too large already — implementer's call, but prefer the existing "ArweavePanel — wired Seeds and Accounts" describe block for consistency with T4 of the prior round) drives a full generate run to `status === "running"` via the file's `FakeWorker`, clicks a different category tab, asserts the dialog appears and NO category switch has happened yet (Seeds area still mounted), clicks "Continue in background", and asserts the category DID switch and the run's worker was NOT terminated. A second case repeats up to the dialog, clicks "Stop generation now" instead, and asserts the worker WAS terminated (`FakeWorker.terminated === true`) and the category switch then proceeded.
  5. `npx vitest run --root packages/codex-arweave` (whole package) shows no new failures beyond the six documented pre-existing `node:sqlite` load failures. `npx tsc --noEmit -p packages/codex-arweave/tsconfig.json` is clean.
  - files: `packages/codex-arweave/src/panel/ArweavePanel.tsx`, `packages/codex-arweave/tests/e5-seeds-area.test.tsx`

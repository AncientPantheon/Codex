## Wave 1
- [ ] T1: Export the toast surface (`txPending` and friends) from `codex-ouronet`'s public `./zbom` entry point, so other packages (`codex-arweave`) can show the standard submit/confirming/confirmed toast without a relative cross-package import. Today `txPending` lives at `packages/codex-ouronet/src/zbom/toast/toastManager.ts` and is only reachable from inside `codex-ouronet` itself (`SendStoaModal.tsx` imports it via a relative path); the public `./zbom` barrel (`packages/codex-ouronet/src/zbom/index.ts`) re-exports `./debouncer`, `./patron`, `./zbomProfiles` but not `./toast`.
  - files: `packages/codex-ouronet/src/zbom/index.ts`, `packages/codex-ouronet/tests/zbom-toast-export.test.ts` (new)
  - done when: `zbom/index.ts` adds `export * from "./toast/toastManager.js";`; new test file imports `txPending` (and the `ToastController`/`ToastEntry` types it needs) via the package's own build output the same way an external consumer would (`import { txPending } from "../src/zbom/index.js"` is acceptable for this in-package test — the point being verified is that the symbol is now reachable from the barrel, not a cross-package resolution which is out of this package's own test runner's reach) and asserts `txPending("test").submitted` and `.fail` are functions; `npx vitest run packages/codex-ouronet/tests/zbom-toast-export.test.ts --root packages/codex-ouronet` passes; `npx tsc --noEmit -p packages/codex-ouronet/tsconfig.json` clean.

- [ ] T2: Add `estimateFee(pool, byteSize, target)` to `arweave-core`, extracted from `tx/transfer.ts`'s existing inline price-fetch block (lines ~197-210) rather than duplicated — `transfer.ts` is changed to call the new function instead of keeping its own inline copy, so there is exactly one implementation of "fetch and validate a Winston price quote through the pool."
  - files: `packages/arweave-core/src/reads/fee.ts` (new), `packages/arweave-core/src/tx/transfer.ts`, `packages/arweave-core/src/index.ts`, `packages/arweave-core/tests/reads-fee.test.ts` (new)
  - done when: `reads/fee.ts` exports `async function estimateFee(pool: GatewayPool, byteSize: number, target: string): Promise<bigint>` that builds a per-endpoint client via `createEndpointClientFactory()` (from `./tx/endpointClient.js`, already used the same way in `transfer.ts`), calls `pool.execute(...)` with `client.transactions.getPrice(byteSize, target)`, validates the raw response against `/^\d+$/` before `BigInt(...)`, and throws `InvalidGatewayPriceError` (imported from `../tx/errors.js`) on a malformed quote — matching `transfer.ts`'s current inline logic exactly; `transfer.ts`'s `sendTransfer` is changed to call `estimateFee(pool, TRANSFER_BYTE_SIZE, params.target)` in place of its own inline `pool.execute` price block; `estimateFee` is exported from `packages/arweave-core/src/index.ts` alongside the existing `getBalance`/`sendTransfer` exports; new test file covers a successful quote, a malformed-quote throw, and pool rotation to a second endpoint on the first endpoint's failure (mirror `reads-balance.test.ts`'s existing test structure for these three cases); `npx vitest run packages/arweave-core/tests/reads-fee.test.ts packages/arweave-core/tests/tx-transfer.test.ts --root packages/arweave-core` passes, with `tx-transfer.test.ts` passing UNMODIFIED (proves the extraction didn't change `sendTransfer`'s observable behavior); `npx tsc --noEmit -p packages/arweave-core/tsconfig.json` clean.

- [ ] T3: Change the playground's default Arweave gateway from the testnet-only placeholder to the real mainnet gateway `https://arweave.net`, and deliberately supersede the four existing "funds-safety N-04" test assertions that currently assert the opposite. Those tests exist specifically to block this exact change (a prior, now-superseded decision to never default to mainnet) — this task's job is to update them to encode the NEW deliberate default, not silently break them or delete the safety net without explanation.
  - files: `apps/codex-playground/src/ArweaveModeToggle.tsx`, `apps/codex-playground/src/networkSettings.ts`, `apps/codex-playground/tests/e5-standalone-smoke.test.tsx`, `apps/codex-playground/tests/e5-toggle-and-backup-roundtrip.test.ts`, `apps/codex-playground/tests/network-settings-wiring.test.tsx`
  - done when: `ArweaveModeToggle.tsx`'s `DEFAULT_GATEWAY_URL` constant (line ~35) is `"https://arweave.net"`; its doc-comment (lines ~28-34, currently asserting "this default NEVER contains 'arweave.net'") is rewritten to state the new invariant — real mainnet reads are the deliberate default, the user-editable "Gateway URL" input in the same component remains the escape hatch for pointing at a testnet/alternate gateway during development; `networkSettings.ts`'s matching comment (lines ~9-10, ~65-67) is updated identically; `e5-standalone-smoke.test.tsx:127`'s `expect(DEFAULT_GATEWAY_URL).not.toContain("arweave.net")` becomes `expect(DEFAULT_GATEWAY_URL).toContain("arweave.net")`; `e5-toggle-and-backup-roundtrip.test.ts`'s equivalent assertion (~line 147) and its preceding comment (~lines 144-146) are updated the same way; `network-settings-wiring.test.tsx`'s `it("never defaults the Arweave gateway to mainnet arweave.net (funds-safety N-04)", ...)` (line ~60) is renamed to describe the new intent and its body asserts `settings.arweaveGatewayUrl` now DOES contain `"arweave.net"`; that same file's `it("names no arweave.net literal in the network-settings wiring", ...)` (line ~156) is removed, since the literal is now intentionally present; `npx vitest run apps/codex-playground/tests/e5-standalone-smoke.test.tsx apps/codex-playground/tests/e5-toggle-and-backup-roundtrip.test.ts apps/codex-playground/tests/network-settings-wiring.test.tsx --root apps/codex-playground` passes; `npx tsc --noEmit -p apps/codex-playground/tsconfig.json` clean.

- [ ] T4: Wire a live Arweave balance into every "Codex Accounts" row, replacing the hardcoded `"Empty"` value cell, and add a refresh control — mirroring `StoaAccountsTab.tsx`'s existing "Live balances / Refresh" convention.
  - files: `packages/codex-arweave/src/panel/ArweaveAccountsArea.tsx`, `packages/codex-arweave/src/panel/ArweavePanel.tsx`, `packages/codex-arweave/tests/e5-accounts-area.test.tsx`
  - done when: `ArweavePanel.tsx` passes `deps.getBalance` into `<ArweaveAccountsArea>` (it is currently explicitly NOT passed, per a code comment near lines 505-525 explaining the omission was provisional pending real display wiring — that comment is removed/updated to reflect this task); `ArweaveAccountsArea.tsx` fires one `getBalance(address)` call per visible row with a resolvable `address` on mount and on refresh, in parallel via `Promise.allSettled` (no bulk-read capability exists); the value cell (`data-testid="arweave-account-value-${entry.id}"`) renders `winstonToAr(balance)` (from `@ancientpantheon/arweave-core`) once resolved, a loading indicator while pending, and a distinct non-crashing fallback (e.g. `"—"` with a `data-testid` suffix `-error`) if that row's read rejects; a new "Live balances" label + refresh icon button (`data-testid="arweave-accounts-refresh"`) re-fires every visible row's read on click; the component exposes a stable per-row refresh trigger usable by a later task after a successful send (e.g. a `refreshBalance(address: string): void` function held in the component, or a per-row nonce bumped on refresh) — name and shape are the implementer's choice, but it must be callable for exactly one row without re-fetching every row; the existing test `"shows a static 'Empty' value cell on every row (balances are out of scope)"` (lines ~322-333) is rewritten to assert a live value instead; new tests cover: a mocked `getBalance` resolving to a formatted AR value, the refresh button re-invoking `getBalance` for visible rows, and a rejected `getBalance` promise rendering the fallback state without throwing; `npx vitest run packages/codex-arweave/tests/e5-accounts-area.test.tsx --root packages/codex-arweave` passes; `npx tsc --noEmit -p packages/codex-arweave/tsconfig.json` clean.

- [ ] T5: Add two new members to `ArweavePanelDeps` — `sendFrom(entry: ForeignKeyEntry, req: ArweaveSendRequest): Promise<ArweaveSendResult>` and `estimateFee(byteSize: number, target: string): Promise<bigint>` — as required fields (matching `send`/`getBalance`'s existing non-optional convention), with a fake implementation of both wired into the playground's mock mode, and matching fixture updates everywhere `ArweavePanelDeps` is constructed in tests so the package keeps compiling.
  - files: `packages/codex-arweave/src/panel/context.tsx`, `apps/codex-playground/src/mockArweaveAdapter.ts`, `packages/codex-arweave/tests/e4-integration-smoke.test.tsx`, `packages/codex-arweave/tests/e4-panel-categories.test.tsx`, `packages/codex-arweave/tests/e4-panel-pure-keys.test.tsx`, `packages/codex-arweave/tests/e5-seeds-area.test.tsx`
  - done when: `context.tsx`'s `ArweavePanelDeps` interface (lines ~119-125, the "balance / send (E2)" group) gains `sendFrom` and `estimateFee` with JSDoc matching the style of `send`/`getBalance`'s existing doc comments on the same lines; `mockArweaveAdapter.ts`'s `buildMockPanelDeps` returns a fake `sendFrom: async () => ({ id: "mock-send-tx", reward: 0n })` (mirroring the existing fake `send`) and a fake `estimateFee: async () => 100000000n` (a plausible fixed Winston quote); each of the four listed `codex-arweave` test files' `ArweavePanelDeps`-typed fixture builders (`makeDeps` in `e4-integration-smoke.test.tsx` line ~134 and `e4-panel-categories.test.tsx` line ~65, `makePanelDeps` in `e4-panel-pure-keys.test.tsx` line ~1158, and all four inline object literals in `e5-seeds-area.test.tsx` at lines ~544, ~669, ~791, and inside `makeFullDeps()` at ~3344) gets a matching `sendFrom: vi.fn(...)` and `estimateFee: vi.fn(...)` so every file keeps compiling and its existing tests keep passing unmodified; `npx tsc --noEmit -p packages/codex-arweave/tsconfig.json` clean; `npx tsc --noEmit -p apps/codex-playground/tsconfig.json` clean; `npx vitest run --root packages/codex-arweave` shows no new failures versus the pre-task baseline (run once before starting to capture the baseline count, quote both counts in the report).

## Wave 2 (depends on Wave 1)
- [ ] T6: Implement the real `sendFrom` and `estimateFee` in the playground's real-mode adapter, and fix `decryptArweaveKey`'s unconditional throw — the actual bug that currently blocks any real send end to end, independent of everything else in this plan.
  - files: `apps/codex-playground/src/realArweaveAdapter.ts`, `apps/codex-playground/tests/e5-foreign-chains-mock.test.tsx`
  - done when: inside `buildRealPanelDeps` (lines ~245-369), `decryptArweaveKey` (currently hardcoded at lines ~320-322 to `throw new Error("decryptArweaveKey requires the unlock-gated keyring.")`) is changed to fall back to `persistence?.decryptArweaveKey`, matching the exact pattern already used by `generateArweaveKey`/`importArweaveKey`/`addForeignKey`/`deleteForeignKey` in the same returned object (i.e. `persistence?.decryptArweaveKey ?? (async () => { throw new Error(...) })`, keeping the throw only for the case where `persistence` itself is unarmed); a new `sendFrom: async (entry, req) => {...}` is added that calls the (now-fixed) `decryptArweaveKey(entry)` to obtain the JWK, then `resolvedAdapter.buildSend({ target: req.target, quantity: req.quantity, maxRewardWinston: req.maxRewardWinston })`, then `resolvedAdapter.post(built, jwk)`, returning its `{id, reward}` result directly, letting any error from any step propagate uncaught (no swallowing); a new `estimateFee: (byteSize, target) => estimateFee(resolvedPool, byteSize, target)` is added, calling T2's new `arweave-core` export directly against `buildRealPanelDeps`'s own `resolvedPool` (the same pattern already used by `pollStatus`/`openUrl`/`rebuildLibrary` in this file, which call `resolvedPool` directly rather than going through the adapter); `realArweaveAdapter.ts`'s existing `send` stub (lines ~333-337) is left untouched — nothing in this plan calls it; new tests added to `e5-foreign-chains-mock.test.tsx` (near the existing "PG-04 — a persisted Arweave key round-trips through the real crypto" describe block, which already exercises `buildRealPanelDeps` with a real `persistence` object) covering: `decryptArweaveKey` returns the real JWK (not a throw) when `persistence` is armed; `sendFrom` calls `decryptArweaveKey`, then `buildSend`, then `post` in that order with the exact params from `req` (assert via mocked/spied `resolvedAdapter.buildSend`/`.post`); a failure at any one of those three steps propagates its original error message, not a generic one; `estimateFee` calls the injected pool and returns its bigint quote. `npx vitest run apps/codex-playground/tests/e5-foreign-chains-mock.test.tsx --root apps/codex-playground` passes; `npx tsc --noEmit -p apps/codex-playground/tsconfig.json` clean.

- [ ] T7: Add a Send button to each Arweave account row and a `SendArweaveModal` it opens — recipient + amount only, live fee estimate, JWK resolved only at submit time, standard toast feedback, matching `SendStoaModal.tsx`'s UX shell adapted for Arweave's decrypt-at-submit signing model (no gas station, no `CodexSigningStrategy`).
  - files: `packages/codex-arweave/src/panel/SendArweaveModal.tsx` (new), `packages/codex-arweave/src/panel/ArweaveAccountsArea.tsx`, `packages/codex-arweave/tests/e5-send-arweave-modal.test.tsx` (new)
  - done when: `SendArweaveModal.tsx` takes `{ entry: ForeignKeyEntry; isOpen: boolean; onClose: () => void; deps: ArweavePanelDeps; onSuccess?: (id: string) => void }`; renders via `CodexModalShell` (imported from `@ancientpantheon/codex-ouronet/ui`, the same import already used in `ArweaveSeedsArea.tsx`'s seed-reveal modal in this package); has exactly two input fields, recipient address and AR amount (no manual fee-cap field); on open and on amount/recipient change, calls `deps.estimateFee(0, recipient)` (byte size `0`, matching `TRANSFER_BYTE_SIZE`'s meaning of a data-less transfer) and displays the result converted via `winstonToAr` as "Network fee: ~X AR", holding the raw bigint internally to compute `maxRewardWinston = quote * 12n / 10n` (a 1.2× buffer) — never shown to the user as an editable field; on submit, wraps the call in `txPending("Send Arweave")` (imported via `@ancientpantheon/codex-ouronet/zbom`, per T1), calls `deps.sendFrom(entry, { target: recipient, quantity: arToWinston(amount), maxRewardWinston })`, calls `_tx.submitted(result.id)` and `onSuccess?.(result.id)` + `onClose()` together on success, and `_tx.fail(message)` on any error — a caught error whose `.name === "CodexLockedError"` (the `isCodexLockedError` duck-type already used in `PureKeysArea.tsx`, reused or mirrored locally in this new file since `codex-arweave` doesn't value-import `codex-ouronet`'s error class) shows "Codex is locked. Unlock your codex to send." specifically, any other error shows its own message, never a generic fallback; `ArweaveAccountsArea.tsx`'s icon-button cluster (copy/explorer/delete) gains a Send button before the cluster's existing entries, `data-testid="arweave-account-send-${entry.id}"`, same 26×26 `iconButtonStyle` sizing as the existing copy/explorer buttons, opening `SendArweaveModal` for that row's `entry`; on the modal's `onSuccess`, the row calls T4's per-row refresh trigger for that row's address; new test file covers: clicking Send opens the modal for the correct row; the fee estimate renders from a mocked `deps.estimateFee`; a successful submit calls `deps.sendFrom` with the exact recipient/quantity/maxRewardWinston, fires the toast, calls `onSuccess`, and closes; a `CodexLockedError` thrown by `sendFrom` shows the specific locked message; a generic thrown error shows its own message; `npx vitest run packages/codex-arweave/tests/e5-send-arweave-modal.test.tsx packages/codex-arweave/tests/e5-accounts-area.test.tsx --root packages/codex-arweave` passes; `npx tsc --noEmit -p packages/codex-arweave/tsconfig.json` clean.

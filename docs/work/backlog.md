- Flaky spec: `codex-ouronet/tests/ui-seed-words-tab.test.tsx > "reveals the decrypted mnemonic only after authentication via useCodexAuth"` fails intermittently (~1 in 3) ONLY under full-suite parallel load at ~1928ms; passes 3/3 in isolation. Pre-existing (file untouched by the class-IA work), but exposure rose once `ui-chainweb-panel.test.tsx` began mounting SeedWordsTab too. Likely a real-decryption vs `waitFor` timeout race — raise that spec's timeout or stub the KDF.
- Load-dependent test flakes in codex-arweave (NOT regressions — both pass when run alone): `e4-treeshake-bundle.test.ts` does ~12.7s of real esbuild work against a 5s per-test timeout, and `e1-foreign-keys-slice.test.ts` times out on a real-RSA case under full-suite parallelism. Raise those two files' timeouts (or mark them serial) rather than letting the suite look red under load.
- `packages/codex-arweave` silently resolved the repo-root hoisted `@ouronet/dalos-crypto@4.0.4` because it declared no dependency on it (fixed: peer >=4.4.0 / dev ^4.4.0). Worth auditing OTHER workspace packages for the same undeclared-import pattern — the root hoist masks it until an API is missing at runtime. NEW INSTANCE FOUND 2026-09-13: `codex-arweave/src/panel/ArweavePanel.tsx` imports `@ancientpantheon/codex-ui` (type-only) with codex-ui absent from its package.json — declare it.
- `apps/codex-playground/src/realArweaveAdapter.ts`'s `buildRealPanelDeps` hardcodes `decryptArweaveKey: async () => { throw ... }` unconditionally, ignoring the `persistence` object it already builds from `getPassword`/`addForeignKey` (whose `createArweaveKeyPersistence` DOES return a working `decryptArweaveKey`) — unlike `generateArweaveKey`/`addForeignKey` in the same return object, which correctly use `persistence?.X`. So a stored key's RSA-parameters panel would throw in `mode: "real"` even with the codex unlocked. Not currently hit (the playground defaults to `mode: "mock"`), found while grounding `arweave-account-rows`. Fix: `deps.decryptArweaveKey: persistence?.decryptArweaveKey ?? (async () => { throw ... })`.
